# SPDX-License-Identifier: Apache-2.0
brew "xcodegen"     # generates ScreenSculpt.xcodeproj from project.yml
brew "swiftlint"    # lint, run in CI with --strict
brew "create-dmg"   # the Homebrew shell script, NOT the npm package of the same name
